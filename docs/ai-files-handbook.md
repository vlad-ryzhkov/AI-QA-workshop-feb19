# AI Files Handbook для QA-инженеров

> Единый справочник по созданию, проверке и поддержке файлов контекста для AI-ассистентов.

**Для AI:** Компактные шаблоны без объяснений:
- `.claude/references/claude-md-template.md` — онбординг
- `.claude/references/qa-agent-template.md` — должностная инструкция
- `.claude/references/skill-template.md` — инструментальный навык

Три файла формируют "рабочее окружение" AI-агента. Думайте о них как о найме нового сотрудника:

| Файл | Аналогия | Назначение |
|------|----------|------------|
| `CLAUDE.md` | Онбординг | Wiki проекта: стек, структура, команды |
| `qa_agent.md` | Должностная инструкция | Культура: mindset, anti-patterns |
| `skills/` | Инструментальный ящик | Инструкции для типовых задач |

```
┌─────────────────────────────────────────────────────────┐
│                      CLAUDE.md                          │
│              (Онбординг — ЧТО мы делаем)                │
└───────────────────────────┬─────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────┐
│                     qa_agent.md                         │
│            (Культура — КАК мы думаем)                   │
└───────────────────────────┬─────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────┐
│                       Skills                            │
│           (Инструменты — КАК мы делаем)                 │
│      /analyze → /testcases → /api-tests                 │
└─────────────────────────────────────────────────────────┘
```

---

# Часть 1: CLAUDE.md — Онбординг проекта

**Аналогия:** Первый день нового сотрудника. Вы даёте ему почитать Wiki проекта.

**Время создания:** 10-15 минут

## Минимальный шаблон

```markdown
# [Название проекта]

## Context
- **Project:** [Краткое описание — что тестируем/разрабатываем]
- **Role:** [Роль AI: Senior QA Engineer / Backend Developer]
- **Language:** [Основной язык: Kotlin, Python, TypeScript]

## Tech Stack (LOCKED)

| Компонент | Технология | BANNED |
|-----------|------------|--------|
| HTTP Client | [Ktor/Requests/Axios] | [Retrofit, OkHttp] |
| Serialization | [Jackson/Pydantic] | [Gson, Moshi] |
| Assertions | [Kotest/pytest] | [JUnit assertEquals] |
| Test Framework | [JUnit 5/pytest] | [TestNG] |

## Project Structure

src/
├── main/           # Продакшн код (⛔ не трогать)
└── test/
    ├── kotlin/     # Автотесты
    └── resources/  # Тестовые данные

## Commands

| Действие | Команда |
|----------|---------|
| Build | `./gradlew build` |
| Test | `./gradlew test` |
| Single test | `./gradlew test --tests "ClassName.methodName"` |

## Conventions

- **Naming:** snake_case в JSON, camelCase в коде
- **Commits:** `feat:`, `fix:`, `test:` префиксы
- **Branches:** `feature/`, `bugfix/`, `test/`
```

## Расширенные секции

### Git Safety (обязательно)
```markdown
## Git Safety (CRITICAL)

⛔ **FORBIDDEN:**
- `git reset --hard`, `git checkout .`, `git clean -fd`
- Удаление веток без подтверждения
- Push в main/master напрямую

✅ **MANDATORY:**
- Backup перед деструктивными операциями
- Подтверждение названия ветки перед push

⚠️ **OVERRIDE:** Слово **DESTROY** для forbidden команд
```

### Token Economy
```markdown
## Token Budget

⚠️ **PAUSE:** При задаче > 20,000 токенов — спроси подтверждение
✅ **Экономия:** Точечное чтение вместо полного сканирования
🔑 **Keyword:** FULL_SCAN для полного анализа проекта
```

### Planning Protocol
```markdown
## Workflow

Для задач > 3 файлов:
1. **Analysis** — прочитай требования, найди неясности
2. **Plan** — предложи план, дождись approval
3. **Execute** — пиши код
4. **Verify** — предложи шаги проверки
```

### Permissions
```markdown
## Permissions

✅ **ALLOWED:** Создавать/редактировать файлы в `src/test/`
⚠️ **ASK FIRST:** Изменения в `build.gradle.kts`, новые зависимости
⛔ **FORBIDDEN:** Изменения в `src/main/`, коммиты без запроса
```

## Примеры Tech Stack по языкам

### Kotlin + Ktor
```markdown
| Компонент | Технология | BANNED |
|-----------|------------|--------|
| HTTP | Ktor Client (CIO) | Retrofit, OkHttp |
| JSON | Jackson (SNAKE_CASE) | Gson |
| Assertions | Kotest shouldBe | assertEquals |
```

### Python + pytest
```markdown
| Компонент | Технология | BANNED |
|-----------|------------|--------|
| HTTP | httpx / requests | urllib |
| JSON | Pydantic | manual parsing |
| Assertions | pytest assert | unittest |
```

### TypeScript + Jest
```markdown
| Компонент | Технология | BANNED |
|-----------|------------|--------|
| HTTP | axios / fetch | request |
| Validation | zod | manual |
| Assertions | Jest expect | chai |
```

## Чек-лист готовности CLAUDE.md

- [ ] Context: проект, роль, язык
- [ ] Tech Stack: что используем + что BANNED
- [ ] Structure: где лежат файлы
- [ ] Commands: build, test, lint
- [ ] Git Safety: forbidden команды
- [ ] Permissions: что можно, что нельзя

---

# Часть 2: qa_agent.md — Должностная инструкция

**Аналогия:** Объясняете новому сотруднику культуру: "Мы параноики по безопасности", "Мы ненавидим Thread.sleep", "Мы пишем тесты до кода".

**Время создания:** 15-20 минут

## Минимальный шаблон

```markdown
# QA Agent Mindset

## Core Principles

1. **Trust No One** — проверяй требования на противоречия
2. **Isolation First** — тесты не зависят друг от друга
3. **Cleanup Always** — удаляй созданные данные

## Anti-Patterns (BANNED)

| ❌ Плохо | ✅ Хорошо | Почему |
|----------|-----------|--------|
| Thread.sleep(5000) | Polling с таймаутом | Flaky тесты |
| Хардкод ID | Генерация через UUID | Коллизии |
| Общие данные между тестами | Уникальные данные | Изоляция |
| Игнорировать cleanup | try-finally | Мусор в БД |

## Quality Gates

Перед сдачей работы проверь:
- [ ] Код компилируется: `./gradlew compileTestKotlin`
- [ ] Тесты изолированы (можно запустить в любом порядке)
- [ ] Нет хардкода (ID, email, телефоны)
- [ ] Cleanup работает (данные удаляются)
```

## Принципы по профилям QA

### Универсальные (для всех)
```markdown
1. **Trust No One** — проверяй требования на противоречия
2. **Isolation First** — тесты не зависят друг от друга
3. **Cleanup Always** — удаляй созданные данные
4. **Fail Fast** — падай рано, падай громко
5. **Evidence-Based** — каждый баг с доказательствами
```

### API Testing
```markdown
1. **Contract First** — тест проверяет контракт, не реализацию
2. **Boundary Obsession** — граничные значения важнее happy path
3. **Negative > Positive** — негативных сценариев больше
4. **Idempotency Check** — повторный запрос = тот же результат
```

### UI/E2E Testing
```markdown
1. **User Perspective** — думай как пользователь
2. **Stable Selectors** — data-testid лучше CSS классов
3. **Visual Regression** — скриншоты критичных экранов
4. **Flaky = Bug** — нестабильный тест — это баг теста
```

### Performance Testing
```markdown
1. **Baseline First** — сначала измерь, потом оптимизируй
2. **Realistic Load** — профиль нагрузки из продакшена
3. **Percentiles > Average** — p95/p99 важнее среднего
4. **Resource Monitoring** — CPU/RAM/IO во время теста
```

### Security Testing
```markdown
1. **OWASP Top 10** — минимальный чек-лист
2. **AuthZ ≠ AuthN** — авторизация и аутентификация разные вещи
3. **Trust Nothing** — все входные данные потенциально вредоносны
4. **Least Privilege** — минимум прав для работы
```

## Расширенные секции

### Security Mindset
```markdown
## Security (CRITICAL)

**Мы параноики. Каждый тест — потенциальная уязвимость.**

⛔ **NEVER:**
- Логировать пароли, токены, персональные данные
- Хардкодить credentials в коде
- Отключать SSL верификацию в проде

✅ **ALWAYS:**
- Credentials через environment variables
- Маскировать чувствительные данные в логах
- Проверять permissions и access control
```

### Test Design
```markdown
## Test Design

**Принцип: Один тест = Один сценарий = Один assert**

### Структура теста (AAA)
@Test
fun `user can register with valid data`() {
    // Arrange — подготовка
    val request = TestData.validRegistration()
    // Act — действие
    val response = api.register(request)
    // Assert — проверка
    response.status shouldBe 201
}

### Naming Convention
`[actor] can [action] when [condition]`
`[actor] cannot [action] when [condition]`

✅ `user can login with valid credentials`
✅ `user cannot register with existing email`
❌ `testLogin` — непонятно что проверяем
```

### Data Management
```markdown
## Test Data

**Принцип: Каждый тест создаёт свои данные и удаляет их**

### Генерация уникальных данных
object TestData {
    private val timestamp = System.currentTimeMillis()
    fun uniqueEmail() = "test_${timestamp}_${UUID.randomUUID()}@test.com"
    fun uniquePhone() = "+7${(1000000000..9999999999).random()}"
}

### Cleanup Pattern
var createdUserId: String? = null
try {
    createdUserId = api.createUser(data).id
    // ... тест ...
} finally {
    createdUserId?.let { api.deleteUser(it) }
}
```

### Cross-Skill Protocol
```markdown
## Cross-Skill Protocol

При использовании skills соблюдай workflow:
1. `/analyze` — сначала проверь требования
2. `/testcases` — затем напиши тест-кейсы
3. `/api-tests` — потом автоматизируй

**Не прыгай сразу к коду!** Анализ экономит время на переделку.
```

## Чек-лист готовности qa_agent.md

- [ ] Core Principles: 3-5 правил мышления
- [ ] Anti-Patterns: таблица "плохо → хорошо"
- [ ] Quality Gates: чек-лист перед сдачей
- [ ] Security: что NEVER, что ALWAYS
- [ ] Test Design: структура, naming
- [ ] Data Management: генерация, cleanup

---

# Часть 3: Skills — Инструментальный ящик

**Аналогия:** Набор инструкций для типовых задач. Нужно протестировать API — достаёт инструкцию "Как писать API-тесты". Нужно проверить UI — берёт другой инструмент.

**Время создания первого skill:** 15-30 минут

## Принцип Progressive Disclosure

Skills используют **трёхуровневую систему** для экономии токенов:

| Уровень | Содержимое | Когда загружается |
|---------|------------|-------------------|
| **1. YAML-заголовок** | Название, описание | **Всегда** в системный промпт |
| **2. Тело SKILL.md** | Полная инструкция | Когда Claude активирует skill |
| **3. Связанные файлы** | scripts/, references/ | По явному запросу |

## Лимит размера: ≤500 строк

**SKILL.md не должен превышать 500 строк.** Если больше — skill становится неэффективным.

### Что выносить

| Что | Куда | Когда |
|-----|------|-------|
| Большие примеры кода (>50 строк) | `references/examples.md` | Загружается по запросу |
| Справочные таблицы | `references/tables.md` | Загружается по запросу |
| Чек-листы проверок | `references/checklist.md` | Загружается по запросу |
| Общие анти-паттерны | `.claude/qa-antipatterns/*.md` | Шарятся между skills |
| Исполняемый код | `scripts/*.py` | Вызывается при необходимости |

### Признаки слишком большого skill

- AI "забывает" инструкции из начала файла
- Генерация занимает слишком много токенов
- Результат не соответствует шаблону из skill

**Решение:** Разбить на несколько skills или вынести в references/

## Структура директории

```
.claude/skills/{skill-name}/
├── SKILL.md              # Уровни 1-2: заголовок + инструкция
├── scripts/              # Уровень 3: исполняемый код (опционально)
│   ├── generate.py
│   └── validate.sh
└── references/           # Уровень 3: документация (опционально)
    ├── api-schema.json
    └── examples.md
```

## Минимальный шаблон SKILL.md

```markdown
---
description: [Краткое описание — что делает этот skill, < 100 символов]
---

# [Название Skill]

## Когда использовать
- [Триггер 1: "напиши api тест"]
- [Триггер 2: "создай автотест для..."]

## Обязательные шаги
1. [Шаг 1]
2. [Шаг 2]
3. [Шаг 3]

## Шаблон вывода
[Пример кода или структуры]

## Чек-лист перед завершением
- [ ] [Проверка 1]
- [ ] [Проверка 2]
```

## Примеры YAML description

**Хорошо:**
```yaml
description: Генерирует API автотесты на Kotlin с Ktor и Kotest
description: Анализирует спецификацию на противоречия и пробелы
description: Проверяет скриншоты на L10n и UI дефекты
```

**Плохо:**
```yaml
description: Этот навык предназначен для автоматической генерации...  # слишком длинно
description: Помогает с тестированием  # слишком абстрактно
description: API тесты  # нет глагола
```

## Примеры skills для QA

| Skill | Назначение | Триггер |
|-------|------------|---------|
| `/analyze` | Аудит требований | "проверь спеку" |
| `/testcases` | Ручные тест-кейсы | "напиши тест-кейсы" |
| `/api-tests` | API автотесты | "напиши автотест" |
| `/screenshot-analyze` | L10n/UI аудит | "проверь скриншот" |

## Связанные файлы: scripts/

**Когда использовать:**
- Генерация boilerplate кода
- Валидация входных данных
- Форматирование вывода

**Пример:**
```python
#!/usr/bin/env python3
"""Генерирует Kotlin DTO из OpenAPI схемы."""

import json
import sys

def generate_dto(schema_path: str) -> str:
    with open(schema_path) as f:
        schema = json.load(f)
    # ... генерация ...
    return kotlin_code

if __name__ == "__main__":
    print(generate_dto(sys.argv[1]))
```

## Связанные файлы: references/

**Когда использовать:**
- Большие примеры кода (>50 строк)
- Справочные таблицы
- Внешние стандарты (OWASP, ISTQB)

**Как ссылаться в SKILL.md:**
```markdown
## Справочные материалы
- Граничные значения: `references/boundary-values.md`
- Коды ошибок HTTP: `references/http-status-codes.md`
```

## Чек-лист готовности skill

- [ ] **Размер:** SKILL.md ≤ 500 строк
- [ ] **YAML-заголовок:** description < 100 символов
- [ ] **Когда использовать:** понятные триггеры
- [ ] **Шаги:** пронумерованы, конкретны
- [ ] **Шаблон вывода:** пример результата
- [ ] **Quality Gates:** чек-лист завершения
- [ ] **Progressive Disclosure:** большие файлы в references/

---

# Часть 4: Поддержка и обслуживание

## Когда обновлять файлы

| Событие | Что обновить |
|---------|--------------|
| Новая библиотека в проекте | CLAUDE.md → Tech Stack |
| Изменилась структура папок | CLAUDE.md → Project Structure |
| Новый anti-pattern в команде | qa_agent.md → Anti-Patterns |
| Повторяющаяся задача | Создать новый skill |
| Skill не работает как надо | Уточнить шаги в SKILL.md |

## Code Review для AI-файлов

При PR с изменениями в `.claude/` или `CLAUDE.md`:

- [ ] Нет конфликтов с существующими правилами
- [ ] BANNED секции актуальны
- [ ] Примеры кода компилируются
- [ ] Нет дублирования между файлами
- [ ] Skills используют Progressive Disclosure

## Версионирование

```markdown
<!-- В конце CLAUDE.md -->
## Changelog
- 2024-01-15: Добавлен Ktor Client в Tech Stack
- 2024-01-10: Обновлены Quality Gates
- 2024-01-05: Создан файл
```

## Типичные ошибки

### 1. Слишком общий контекст
```markdown
❌ "Мы пишем тесты на Kotlin"
✅ "E2E API тесты для сервиса регистрации. Kotlin 1.9, Ktor Client, Kotest matchers."
```

### 2. Нет примеров кода
```markdown
❌ "Используй наш стиль написания тестов"
✅ "Пример теста:" + блок кода
```

### 3. Нет запретов
```markdown
❌ "Пиши хороший код"
✅ "BANNED: Thread.sleep, raw assertions, var instead of val"
```

### 4. Абстрактные шаги в skills
```markdown
❌ "1. Проанализируй  2. Сгенерируй  3. Проверь"
✅ "1. Прочитай спецификацию, найди request/response схемы
    2. Сгенерируй DTO с @JsonNaming(SnakeCaseStrategy::class)
    3. Проверь компиляцию: ./gradlew compileTestKotlin"
```

### 5. Всё в одном файле skill
```markdown
❌ [500 строк кода примеров в SKILL.md]
✅ Основная инструкция + ссылки на references/
```

---

# Часть 5: Bootstrap Kit — Автоматизация создания

## Meta-Skills

Вместо ручного создания файлов используйте meta-skills:

```
┌─────────────────────────────────────────────────────────┐
│              Meta-Skills (Bootstrap Kit)                │
│                                                         │
│  /init-project  →  сканирует репо → создаёт CLAUDE.md  │
│  /init-agent    →  спрашивает профиль → qa_agent.md    │
│  /init-skill    →  создаёт новый skill с структурой    │
│                                                         │
│  Один артефакт = инструкция человеку + prompt для AI   │
└─────────────────────────────────────────────────────────┘
```

## Использование

| Команда | Что делает | Результат |
|---------|------------|-----------|
| `/init-project` | Сканирует build файлы, структуру, CI | `CLAUDE.md` |
| `/init-agent` | Спрашивает профиль QA | `.claude/qa_agent.md` |
| `/init-skill` | Создаёт skill с Progressive Disclosure | `.claude/skills/{name}/` |

## Quick Start с Bootstrap Kit (5 минут)

```
User: /init-project
AI: [Сканирует проект, генерирует CLAUDE.md]

User: /init-agent
AI: Какой профиль? → Mixed
AI: [Генерирует qa_agent.md]

User: /init-skill
AI: Что делает skill? → Проверяет naming convention
AI: [Создаёт /lint-test-names]
```

---

# Часть 6: Продвинутые подходы

Когда базовая конфигурация (CLAUDE.md + qa_agent.md + Skills) уже работает, можно усилить AI-окружение дополнительными инструментами.

| Подход | Суть | Когда полезен |
|--------|------|---------------|
| **MCP серверы** | Расширяют capabilities AI: актуальные доки (context7), глубокий анализ (sequential-thinking) | Когда AI использует устаревшие API или нужен пошаговый разбор |
| **CI/CD с Claude** | GitHub Actions для автоматического code review на PR | Командная работа, enforce standards на PR |
| **Skill lint** | Автоматическая валидация SKILL.md: frontmatter, broken links, naming | Когда скиллов > 5 и нужен quality gate |
| **Symlink-стратегия** | Один source файл → симлинки для Claude/Cursor/Copilot/Gemini | Когда нужна поддержка 4+ IDE без дублирования (альтернатива wrapper-подходу) |
| **Reference-Driven Anti-Patterns** | Один файл = один анти-паттерн с Bad/Good примерами, масштабируется до 30+ файлов | Production-проекты с большой командой |
| **Markdownlint** | Автоматическая проверка качества markdown документации | Когда документация становится критичным артефактом |

### MCP серверы

MCP (Model Context Protocol) позволяет AI-агентам обращаться к внешним сервисам во время работы. Конфигурируется через `.mcp.json` в корне проекта.

**Типичные серверы:**
- **context7** — подтягивает актуальную документацию библиотек вместо устаревших знаний модели
- **sequential-thinking** — пошаговые рассуждения для сложных архитектурных решений

Можно отключить для экономии токенов на простых задачах.

### CI/CD с Claude

GitHub Actions workflow, который автоматически запускает Claude для code review на каждый PR. AI использует skills проекта и знает контекст из CLAUDE.md.

**Когда внедрять:** Когда в команде > 1 человека и нужен consistent review quality.

### Markdownlint

Автоматическая валидация markdown-файлов. Конфигурируется через `.markdownlint.yaml`. Рекомендуется selective-подход: `default: false` + явное включение нужных правил.

**Минимальный набор правил:** headings increment, code block language, table consistency, trailing whitespace.

---

# Приложение: Адаптация под другие инструменты

| Инструмент | Файл | Особенности |
|------------|------|-------------|
| **Claude Code** | `CLAUDE.md` | Поддержка skills в `.claude/skills/` |
| **Cursor** | `.cursorrules` | Добавь `globs:` для автоактивации |
| **GitHub Copilot** | `.github/copilot-instructions.md` | Глобальный контекст |
| **Cline/Roo Code** | `.clinerules` или `.roo/rules.md` | Поддержка `.mdc` формата |

---

# Приложение: Промпты для генерации файлов

> Используй эти вопросы чтобы AI сгенерировал файлы для другого проекта

1. "Создай CLAUDE.md для [тип проекта]. Опиши контекст, стек и принципы работы."

2. "Сформируй qa_agent.md для команды QA с профилем [API/UI/Performance/Security/Mixed]."

3. "Создай skill для [повторяющаяся задача]. Используй Progressive Disclosure."

4. "Адаптируй эти файлы под проект с [особенность: legacy код / строгий compliance]."

5. "Сгенерируй минимальный набор файлов для быстрого старта нового проекта."

---

# Quick Reference

## Минимальный набор за 30 минут (вручную)

1. **CLAUDE.md** (10 мин): Context + Tech Stack + Structure
2. **qa_agent.md** (10 мин): 3 правила mindset + 5 anti-patterns
3. **Один skill** (10 мин): Самая частая задача команды

## Минимальный набор за 5 минут (Bootstrap Kit)

```
/init-project → /init-agent → готово
```

## Чек-лист: Готово ли окружение?

- [ ] **CLAUDE.md** содержит стек, структуру проекта, команды сборки
- [ ] **qa_agent.md** описывает mindset и anti-patterns
- [ ] **Skills** созданы для повторяющихся задач
- [ ] В skills используется progressive disclosure
- [ ] Запреты помечены словами BANNED/FORBIDDEN
- [ ] Есть примеры кода, а не только текст
