# Гайд: Как создать идеальный CLAUDE.md

Этот документ объясняет структуру файла конфигурации `CLAUDE.md` (или `.cursorrules`).
Хороший контекстный файл превращает AI в более надёжного помощника вашего проекта.

---

## Структура файла

### 1. PROJECT CONTEXT (Контекст проекта)

Самая важная часть. Опишите "Где мы" и "На чём пишем".

```markdown
## Context
- **Project:** E2E тесты для сервиса Х авторизации, платежей и тд
- **Stack:** Kotlin 1.9, JUnit 5, Ktor Client, Allure TestOps
- **Internal libs:** используем общую библиотеку `test-utils`
```

**Что включить:**
- Название проекта и его назначение
- Технологический стек (язык, версия, фреймворки)
- Внутренние библиотеки и их назначение
- Роль AI (QA Engineer, Backend Developer, etc.)

---

### 2. ANTI-OVERENGINEERING (Правила "Чего НЕ делать")

AI любит усложнять. Ограничьте его фантазию жёсткими запретами. Используйте слова **BANNED**, **FORBIDDEN**, **CRITICAL**.

```markdown
## Anti-Patterns

⛔ **BANNED:**
- Создавать свои HTTP-клиенты (используй `ApiClient`)
- Использовать `Thread.sleep()` (используй polling)
- Писать комментарии к очевидному коду
- Использовать `var` там, где можно `val`
- Хардкодить тестовые данные в тестах

✅ **REQUIRED:**
- Data classes для всех DTO
- try-finally для cleanup
- Уникальные данные через timestamp/UUID
```

**Почему это важно:**
- AI без ограничений создаст "идеальную" архитектуру из 15 абстракций
- Явные запреты экономят время на code review
- Слова BANNED/FORBIDDEN работают лучше, чем "не рекомендуется"

---

### 3. CORE PATTERNS (Архитектурные паттерны)

Покажите AI примеры "идеального кода". Лучше один раз показать `data class`, чем 10 раз объяснять словами.

```markdown
## Code Patterns

### HTTP Request
```kotlin
// ✅ GOOD
val response = apiClient.post("/users/register", payload)
response.status shouldBe 201

// ❌ BAD
val response = httpClient.get("url") // Don't use raw client
assert(response.code == 200) // Don't use raw assertions
```

### DTO Naming
```kotlin
// Request: мы контролируем контракт
data class RegisterRequest(
    @JsonProperty("full_name") val fullName: String
)

// Response: устойчивость к изменениям
@JsonIgnoreProperties(ignoreUnknown = true)
data class RegisterResponse(val id: String)
```
```

**Что включить:**
- Как выглядит базовый HTTP запрос
- Как вы пишете DTO (SnakeCase vs CamelCase)
- Как организованы файлы
- Пример Polling/Retry логики

---

### 4. FILE STRUCTURE (Структура папок)

Помогите AI понять, куда класть новые файлы.

```markdown
## Project Structure
```text
src/test/kotlin/com/example/
  ├── api/
  │   ├── models/      # Request/Response DTOs
  │   └── clients/     # API клиенты
  ├── data/            # Test Data (Object Mother)
  ├── helpers/         # Auth, cleanup, utilities
  └── tests/           # Сами тесты
```
```

**Зачем:**
- AI не будет создавать файлы в случайных местах
- Консистентность между сгенерированным и существующим кодом

---

### 5. LOCAL SETUP & GOTCHAS (Нюансы)

Опишите специфические проблемы вашего языка или окружения.

```markdown
## Gotchas

### Kotlin
- Используй экранирование `\$` в строках, если за ним идёт переменная
- Для Allure шагов используй `Allure.ThrowableRunnableVoid`

### Environment
- Переменная `API_BASE_URL` должна быть в `.env`
- Для локального запуска нужен VPN
```

---

### 6. AUTONOMY (Уровень свободы)

Задайте границы дозволенного.

```markdown
## Permissions

✅ **ALLOWED:**
- Создавать и редактировать файлы в `src/test/`
- Запускать тесты через `./gradlew test`

⚠️ **ASK FIRST:**
- Изменения в `build.gradle.kts`
- Добавление новых зависимостей

⛔ **FORBIDDEN:**
- Коммиты в ветку `main`
- Удаление файлов без подтверждения
- Изменения в `src/main/`
```

---

## Чек-лист: Хороший ли у вас CLAUDE.md?

- [ ] **Стиль:** AI использует ваши naming conventions (snake_case в JSON)?
- [ ] **Библиотеки:** AI не тянет OkHttp, если у вас Ktor?
- [ ] **Изоляция:** AI пишет изолированные тесты, не хардкодит данные?
- [ ] **Структура:** Новые файлы создаются в правильных папках?

---

## Полный пример CLAUDE.md

```markdown
# Project: User Service Tests

## Context
- **Project:** API тесты для сервиса пользователей
- **Role:** Senior SDET
- **Stack:** Kotlin 1.9, JUnit 5, Ktor Client, Kotest, Allure

## Anti-Patterns

⛔ **BANNED:**
- Thread.sleep() — используй polling
- Raw HTTP clients — используй ApiClient
- var где можно val
- Комментарии к очевидному коду

✅ **REQUIRED:**
- Data classes для DTO
- try-finally для cleanup
- Unique test data via timestamp

## Code Patterns

### Test Structure
```kotlin
@Test
fun `user can register`() = runTest {
    val payload = TestData.validRequest()
    var userId: String? = null

    try {
        val response = apiClient.register(payload)
        response.status shouldBe 201
        userId = response.body.id
    } finally {
        userId?.let { apiClient.deleteUser(it) }
    }
}
```

## File Structure
```
src/test/kotlin/com/example/
  ├── api/models/
  ├── api/clients/
  ├── data/
  └── tests/
```

## Permissions
- ✅ Edit files in src/test/
- ⛔ Never commit to main
```

---

## Адаптация под другие инструменты

| Инструмент | Файл | Особенности |
|------------|------|-------------|
| **Claude Code** | `CLAUDE.md` | Поддержка skills в `.claude/skills/` |
| **Cursor** | `.cursorrules` | Добавь `globs:` для автоактивации |
| **GitHub Copilot** | `.github/copilot-instructions.md` | Глобальный контекст |
| **Cline/Roo Code** | `.clinerules` или `.roo/rules.md` | Поддержка `.mdc` формата |

---

## Частые ошибки

### 1. Слишком общий контекст
```markdown
// ❌ Плохо
"Мы пишем тесты на Kotlin"

// ✅ Хорошо
"E2E API тесты для сервиса регистрации. Kotlin 1.9, Ktor Client, Kotest matchers."
```

### 2. Нет примеров кода
```markdown
// ❌ Плохо
"Используй наш стиль написания тестов"

// ✅ Хорошо
"Пример теста:" + блок кода
```

### 3. Нет запретов
```markdown
// ❌ Плохо
"Пиши хороший код"

// ✅ Хорошо
"BANNED: Thread.sleep, raw assertions, var instead of val"
```

---

## Опциональные секции

### 7. WORKFLOW (Doc Before Code)

Если хотите строгий порядок работы AI, добавьте:

```markdown
## Workflow

🚨 STOP BEFORE YOU CODE:
1. **Analyze:** Прочитай требования/спецификацию полностью
2. **Plan:** Выдели acceptance criteria и тест-сценарии
3. **Verify:** Убедись что понял задачу (задай вопросы если нужно)
4. **Execute:** Только потом пиши код

⚠️ Никогда не начинай писать код, не поняв контекст.
```

**Когда полезно:**
- Сложные фичи с неявными зависимостями
- Работа с legacy кодом
- Критичные бизнес-флоу (платежи, авторизация)
