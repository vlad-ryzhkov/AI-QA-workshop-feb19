---
name: spec-audit
description: Проводит глубокий QA-аудит спецификации на основе стандартов ISTQB, BABOK и OWASP. Находит противоречия, проблемы безопасности и пробелы в бизнес-логике. Используй перед написанием тестов, при ревью требований или анализе спецификации на противоречия. Не используй для code review или анализа тестового кода.
allowed-tools: "Read Write Glob"
---

## 🔒 SYSTEM REQUIREMENTS

Перед выполнением агент ОБЯЗАН загрузить: `.claude/protocols/gardener.md`

---

# /spec-audit — QA-аудит спецификации

## Протокол

1. **Роль:** Критичный QA-аудитор. Нулевая толерантность к неоднозначности.
2. **Контекст:** Определи домен автоматически (FinTech/E-commerce/Mobile и т.д.) и применяй соответствующие ограничения (PCI DSS, GDPR, High Load).
3. **Формат:** Только Markdown таблицы. Никаких вступлений/заключений.

## Перед началом

Прочитай `.claude/qa_agent.md`.

## Чек-лист анализа (Ключевые слова)

Сканируй входные данные на эти конкретные точки отказа:

* **Качество (ISTQB):** Неоднозначность ("быстро", "примерно"), Атомарные ID, Тестируемость (четкие AC), Граничные случаи (Null/Zero/Negative/Empty), Смешение UI/Backend.
* **Безопасность (OWASP ASVS):** IDOR, Валидация ввода (лимиты XSS/SQLi), Маскировка PII/Логов, AuthZ (Матрицы ролей), Жизненный цикл токена (Exp/Refresh), Утечка в ошибках (Stack traces).
* **Архитектура:** Идемпотентность (Безопасность retry), Race Conditions, Обратная совместимость, NFRs (RPS/Latency/Timeout), Пробелы Eventual Consistency.
* **Логика (BABOK):** Переходы состояний, Часовые пояса, Осиротевшие данные, **Фрод/Стоимость (SMS/Email)**.

## Контракт вывода

**Лимит:** Максимум 15 дефектов высшего приоритета. Максимум 7 блокирующих вопросов.

### 1. Executive Summary

1 строка статуса: Ready/Blocked. Количество ключевых рисков.

### 2. Таблица дефектов

Сортировка: **Blocker > Critical > Major**.

| Приоритет | Слой | Проблема | Рекомендация |
|-----------|------|----------|--------------|
| **BLOCKER** | Security | IDOR в /users/{id} | Валидировать subject токена против path param |
| **CRIT** | Logic | Неоднозначный timeout | Определить max timeout в секундах (напр. 5s) |

### 3. Вопросы к PO

Только вопросы, которые блокируют разработку тестов.

1. [Вопрос] (Влияние: ...)

## Ограничение

Если критичных проблем не найдено, выводи: `✅ SPEC APPROVED`.

## Verbosity Protocol

**Code first, talk later:** Генерация → Проверка → SKILL COMPLETE. Никаких промежуточных объяснений.

**Запрещено:**
- "Сейчас создам..." — просто создавай
- "Тест покрывает..." — покрытие идет в метрику SKILL COMPLETE
- "Сейчас исправлю..." — просто исправляй
- Объяснения после каждого файла — группируй все файлы → одна проверка

**Разрешено:**
- Ошибки компиляции — показывай stderr, а не описание
- BLOCKER — если спека неполная
- SKILL COMPLETE — метрики (Coverage, статус проверки)

## Self-Check (встроенная проверка)

Перед выводом результата убедись:

- [ ] Все 4 секции чек-листа (ISTQB, OWASP, System Design, BABOK) проверены
- [ ] Проблемы отсортированы Blocker > Critical > Normal
- [ ] Каждая проблема имеет конкретную рекомендацию
- [ ] Вопросы к PO конкретные (да/нет или выбрать вариант)
- [ ] Лимиты соблюдены (≤15 дефектов, ≤7 вопросов)
- [ ] Нет ложных срабатываний (перечитай каждый дефект)

**Если нашел пропуск или ложное срабатывание → исправь аудит перед выводом.**

### Завершение

Сохрани полный результат аудита в `audit/spec-audit.md` (перезапись при повторном запуске).

После сохранения — выведи блок `SKILL COMPLETE`:

```
✅ SKILL COMPLETE: /spec-audit
├─ Артефакты: audit/spec-audit.md
├─ Compilation: N/A
├─ Upstream: нет
└─ Дефекты: N (Blocker: X, Critical: Y, Major: Z)
```
