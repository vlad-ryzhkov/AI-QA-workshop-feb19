---
name: test-plan
description: Создаёт приоритизированный план тестового покрытия на основе repo-scout-report. Определяет gaps в спеках, приоритеты endpoints и roadmap. Используй после /repo-scout для планирования QA. Не используй без recon-report.
allowed-tools: "Read Write Edit Glob Grep"
---

# /test-plan — План тестового покрытия

<purpose>
На основе repo-scout-report оценивает API surface, находит пробелы в спецификациях,
приоритизирует endpoints для тестирования и создаёт actionable roadmap
для покрытия сервиса тестами.
</purpose>

## Когда использовать

- После `/repo-scout` — для планирования тестового покрытия
- При входе в новый сервис — "с чего начать тестирование?"
- При ревью тестовой стратегии — "что ещё не покрыто?"

## Когда НЕ использовать

- Без `audit/repo-scout-report.md` — сначала `/repo-scout`
- Для написания тестов — используй `/api-tests`
- Для аудита спецификации — используй `/analyze`

## Входные данные

- `audit/repo-scout-report.md` (от `/repo-scout`) — **ОБЯЗАТЕЛЬНО**
- Спецификации (swagger, proto) — если указаны в recon-report
- CLAUDE.md целевого репо — если есть

## Алгоритм

### Перед началом

Прочитай `.claude/qa_agent.md` (если есть в рабочем проекте). Выведи:

```
📋 TASK BRIEF
├─ Target: {repo-name} — план тестового покрытия
├─ Scope: {N REST + M gRPC endpoints} из repo-scout-report
├─ Constraint: Приоритизация по бизнес-критичности
└─ Action: Вызываю /test-plan...
```

### Фаза 1: Intake

1. Прочитай `audit/repo-scout-report.md`
2. Если файл не найден:
   ```
   🚨 BLOCKER: audit/repo-scout-report.md не найден.
   Сначала запусти /repo-scout для целевого репозитория.
   ⏸️ Жду запуска /repo-scout.
   ```
3. Извлеки:
   - Полный список endpoints (REST + gRPC) из секции 2
   - Specification coverage из секции 3
   - Existing tests из секции 4
   - Infrastructure context из секции 5
   - Readiness assessment из секции 7

4. Если есть спецификации — прочитай ключевые (swagger, proto) для понимания business logic

### Фаза 2: Gap Analysis

**Specification Gaps:**

Для каждого endpoint из API Surface Catalog проверь:
- Есть ли спецификация? (swagger/proto описывает этот endpoint)
- Полная ли спецификация? (response schemas, error codes, auth requirements)
- Актуальна ли? (дата модификации vs код)

Классифицируй:
- **No Spec:** endpoint есть в коде, но нет в swagger/proto
- **Partial Spec:** endpoint есть, но без response schemas или error codes
- **Stale Spec:** endpoint изменился в коде, но spec не обновлён
- **Complete:** спецификация полная и актуальная

**Test Coverage Gaps:**

Для каждого endpoint проверь наличие тестов:
- Unit тесты handler'а
- Integration тесты
- E2E/API тесты

### Фаза 3: Prioritization

Прочитай `references/priority-model.md` для деталей модели.

Для каждого endpoint рассчитай приоритет по формуле:

```
Score = (Public × 3) + (HasSpec × 2) + (NoTests × 1) + (Critical × 2) + (SecurityRisk × 1)

Где:
- Public: 1 = публичный endpoint, 0 = внутренний/admin
- HasSpec: 1 = есть спецификация, 0 = нет
- NoTests: 1 = нет тестов, 0 = есть тесты
- Critical: 1 = бизнес-критичный (auth, payment, registration), 0 = обычный
- SecurityRisk: 1 = обрабатывает PII/auth/payments, 0 = нет

Max Score = 9 (публичный + есть спек + нет тестов + критичный + security)
```

Распредели по приоритетам:
- **P0 (Score 7-9):** Smoke — покрыть первыми
- **P1 (Score 4-6):** Core Business — основное покрытие
- **P2 (Score 1-3):** Edge Cases — расширенное покрытие

### Фаза 4: Architecture Recommendation

На основе данных из recon-report определи:

**Где размещать тесты:**

| Ситуация | Рекомендация |
|----------|-------------|
| Уже есть внешний тест-репо (indrive-api-tests-*) | Работать в существующем |
| Нет тест-репо, сервис с REST API | Создать отдельный Kotlin тест-проект |
| Нет тест-репо, только gRPC | Создать отдельный Go тест-проект или Kotlin с gRPC |
| Простой сервис с 1-5 endpoints | Можно тесты в том же репо |

**Тест-стек (по умолчанию):**

| Компонент | Технология |
|-----------|-----------|
| Language | Kotlin |
| HTTP Client | Ktor Client (CIO) |
| gRPC Client | grpc-kotlin |
| Assertions | Kotest matchers |
| Framework | JUnit 5 |
| Reporting | Allure |

### Фаза 5: Roadmap Assembly

Собери execution roadmap с конкретными шагами и ссылками на скиллы.

## Формат вывода

Сохрани в `audit/test-plan.md`:

```markdown
# Test Coverage Plan: {repo-name}

> Сгенерировано: {дата} | Скилл: /test-plan
> Источник: audit/repo-scout-report.md

## 1. Scope Summary

| Метрика | Значение |
|---------|----------|
| Total Endpoints | {N REST + M gRPC = total} |
| Spec Coverage | {X}/{total} = {%} |
| Existing Tests | {Y unit + Z integration + W e2e} |
| Untested Endpoints | {count} |

## 2. Specification Gaps

### Отсутствуют спеки (нужно создать/получить)

| # | Endpoint | Источник (код) | Приоритет | Action |
|---|----------|---------------|-----------|--------|

### Неполные спеки (нужно обогатить перед /analyze)

| # | Spec File | Проблема | Action |
|---|-----------|----------|--------|

### Рекомендация

{Что делать: попросить команду дописать, генерировать из кода, etc.}

## 3. Test Priority Matrix

### P0 — Smoke ({N} endpoints, Score 7-9)

| # | Endpoint | Score | Обоснование | Spec |
|---|----------|-------|-------------|------|

Формула: Score = (Public × 3) + (HasSpec × 2) + (NoTests × 1) + (Critical × 2) + (SecurityRisk × 1)

### P1 — Core Business ({N} endpoints, Score 4-6)

| # | Endpoint | Score | Обоснование | Spec |
|---|----------|-------|-------------|------|

### P2 — Edge Cases ({N} endpoints, Score 1-3)

| # | Endpoint | Score | Обоснование | Spec |
|---|----------|-------|-------------|------|

## 4. AI Setup Roadmap

| # | Шаг | Скилл | Статус | Артефакт |
|---|-----|-------|--------|----------|
| 1 | Разведка репо | /repo-scout | ✅ Done | audit/repo-scout-report.md |
| 2 | План покрытия | /test-plan | ✅ Done | audit/test-plan.md |
| 3 | CLAUDE.md для тест-проекта | /init-project | {⬜ TODO / ✅} | CLAUDE.md |
| 4 | QA Agent | /init-agent | {⬜ TODO / ✅} | .claude/qa_agent.md |
| 5 | Аудит P0 спек | /analyze | ⬜ TODO | audit/*_audit.md |
| 6 | Тест-кейсы P0 | /testcases | ⬜ TODO | src/test/testCases/ |
| 7 | Автотесты P0 | /api-tests | ⬜ TODO | src/test/kotlin/ |

## 5. Test Architecture

### Размещение тестов

{Рекомендация: отдельный репо / в том же репо / существующий тест-репо}

### Структура тест-проекта

```text
{Предложенное дерево директорий}
```

### Tech Stack

| Компонент | Технология |
|-----------|-----------|
{Таблица стека}

## 6. Execution Roadmap

### Фаза 1: Foundation ({оценка времени})
- [ ] Создать/настроить тест-проект
- [ ] `/init-project` → CLAUDE.md
- [ ] `/init-agent` → qa_agent.md
- [ ] Настроить CI для тестов

### Фаза 2: P0 Smoke ({оценка})
Для каждого P0 endpoint:
- [ ] `/analyze` на спецификацию
- [ ] `/testcases` → мануальные тест-кейсы
- [ ] `/api-tests` → автотесты

### Фаза 3: P1 Core Business ({оценка})
{Аналогично Фазе 2 для P1}

### Фаза 4: P2 Edge Cases ({оценка})
{Аналогично для P2}

## 7. Risks & Blockers

| # | Risk | Impact | Mitigation |
|---|------|--------|------------|
```

## Quality Gates

- [ ] repo-scout-report.md прочитан и все endpoints учтены
- [ ] Каждый endpoint из API Surface появляется в Priority Matrix
- [ ] Score рассчитан по формуле (показана формула с числами)
- [ ] Specification Gaps согласованы с Spec Inventory из recon-report
- [ ] AI Setup Roadmap содержит конкретные шаги со скиллами
- [ ] Нет placeholder'ов `{xxx}` в финальном отчёте

## Self-Check

Перед сохранением проверь:

- [ ] **Completeness:** Все 7 секций заполнены?
- [ ] **Math:** Сумма endpoints в P0+P1+P2 = total из Scope Summary?
- [ ] **Actionable:** Каждый gap имеет конкретный Action?
- [ ] **Realistic:** Roadmap учитывает текущий Readiness Assessment?

## 🔄 Self-Review Protocol (ОБЯЗАТЕЛЬНЫЙ)

После генерации test-plan.md выполни self-review:

1. Перечитай план как новый документ
2. Проверь: все ли endpoints из recon-report учтены?
3. Проверь: корректны ли Score расчёты?
4. Сформируй `audit/test-plan_self_review.md`

### Формат Self-Review

```markdown
# Self-Review: Test Coverage Plan

## Scorecard

**Endpoint Coverage:** X/Y endpoints в Priority Matrix = NN%
**Score Accuracy:** Формулы корректны: ✅/❌
**Actionable Gaps:** X/Y gaps с Action = NN%
**Roadmap Completeness:** X/Y шагов конкретны = NN%

### Итоговый Score: NN%

## Что упущено
- [ ] {Конкретный endpoint/gap}

## Рекомендации
1. {Конкретное улучшение}
```

## Завершение

После сохранения обоих файлов — напечатай блок `SKILL COMPLETE`:

```
✅ SKILL COMPLETE: /test-plan
├─ Артефакты: audit/test-plan.md
├─ Self-Review: audit/test-plan_self_review.md
├─ Compilation: N/A
├─ Upstream: audit/repo-scout-report.md
└─ Score: {итоговый % из self-review}
```

## Связанные файлы

- Модель приоритизации: `references/priority-model.md`
- Вход: `/repo-scout` → `audit/repo-scout-report.md`
- Выход → `/init-project`, `/init-agent`, `/analyze`, `/testcases`, `/api-tests`
