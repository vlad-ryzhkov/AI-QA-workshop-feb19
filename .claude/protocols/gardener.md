# 🌱 Gardener Protocol

> SSOT. Подключается через `SYSTEM REQUIREMENTS` в SKILL.md и `Protocol Injection` в agents.

## Триггеры

Активируется когда:
1. Пользователь нашёл ошибку в output скилла
2. Auditor нашёл ошибку, требующую >1 итерации
3. Обнаружен анти-паттерн, отсутствующий в `qa-antipatterns/`

**НЕ активируется:** штатные compilation fix, разовые опечатки, user preference изменения.

## Алгоритм

Оценить: "Могла ли обновлённая документация предотвратить этот класс ошибок?"
Если ДА — вывести Suggestion. Если НЕТ — молчать.

## Routing

| Класс ошибки | Target |
|---|---|
| Повторяющийся паттерн в коде | `qa-antipatterns/{name}.md` |
| Неверный формат/структура output скилла | `skills/{skill}/SKILL.md` |
| Неверное решение агента | `agents/{agent}.md` |
| Глобальное соглашение | `CLAUDE.md` или `qa_agent.md` |

## Формат вывода

```text
🌱 GARDENER SUGGESTION
├─ Target: [файл]
├─ Root Cause: [одна строка]
└─ Patch: [≤3 строк diff]
```

Только suggestion — применять после подтверждения пользователя.
